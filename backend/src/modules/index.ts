/**
 * Game Logic Modules
 * 
 * This index file exports all the modules for the leg-only game logic engine
 */

import validator from './validator';
import statsTracker from './statsTracker';
import MatchManager from './matchManager';

export { 
  validator,
  statsTracker,
  MatchManager
};

export default {
  validator,
  statsTracker,
  MatchManager
}; 